int sad(int q, int c, int d, int a){
  return a+c;
}

int main (){
  float a <= 5.56;
  char v;
  
  output a;
  a<<6;
  output 10.5;
  if (a == 100) { float b <= 10.0; };
  int q;
  q = 9;
  sad(q,c,d,a);
  test_func1(idade, nome);
  return a + c;
  test_func2[5+3](idade, nome);
}
  
int inc(int as, int bs){
  return as+bs;
  }
  
float trial(float ver){
  int a <= 4;
 	input a;
 	break;
 	
 }
